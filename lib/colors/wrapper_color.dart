import "package:flutter/material.dart";

const wrapperBackground = BoxDecoration(
    gradient: LinearGradient(
        begin: Alignment.topLeft,
        end: Alignment.bottomCenter,
        colors: [Colors.white, Colors.greenAccent]));
